# Dependencies
node_modules/

# Build output
.next/
out/

# Generated blog data is intentionally committed (generated by
# scripts/build-blog.mjs on every build, regenerated in CI, never
# hand-edited):
#   data/blog/posts.json

# Logs / OS / editor noise
*.log
.DS_Store
.idea/
.vscode/

# Local benchmark output (bench:loading writes its medians here; the
# committed record lives in the ROADMAP, not in a regenerable artifact)
/bench-results.json

# TypeScript incremental build cache
*.tsbuildinfo
