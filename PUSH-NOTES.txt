Parsa Tak WEB — full update package (2026-08-27)
================================================

WHAT THIS IS
All changed files from the full-repository performance & quality audit,
ready to copy over your repo root and push. Paths are repo-relative —
merge the folders over your repository root, then commit and push.

FILES (17)
  app/globals.css                  header/modal fix + reader modernization layer
                                   + mobile preview-tags overflow fix
  app/layout.tsx                   preconnect to cdn.jsdelivr.net (faster first media request)
  components/LibraryPdfReader.tsx  persistent reading position, sliding nearby-pages
                                   nav window (byte-budget thumbnails), modernized UI:
                                   footer scrubber, fullscreen, double-click zoom,
                                   PageUp/PageDown, motion polish
  components/LivingShell.tsx       module-level GitHub link, stable scene-change callback
  components/PublicLinks.tsx       module-level link groups (no per-render rebuilds)
  components/RedCursor.tsx         hover-selector hoisted out of the event path
  components/RedMagic.tsx          FIXED compile errors (duplicate declarations);
                                   baked glow sprite now actually used per frame;
                                   resize no longer rebuilds the particle world
                                   unless the quality band changes
  components/ScenePreloader.tsx    shared idle scheduler, connection-aware preloading
  components/scenes/HomeScene.tsx  module-level GitHub link
  components/scenes/LibraryScene.tsx  one-pass filter counts + mobile tags fix (markup unchanged)
  components/scenes/WorkScene.tsx  module-level GitHub link
  lib/contentRepository.ts         manifest parsed once, cached ContentItem list
  lib/idleScheduler.ts             NEW FILE (required by ScenePreloader + PDF reader)
  lib/links.ts                     GITHUB_LINK resolved once at module load
  next-env.d.ts                    regenerated by Next.js 16
  tsconfig.json                    updated by Next.js 16 (jsx: react-jsx + formatting)
  worklog.md                       rebuilt agent-handoff document (open items only)

HOW TO APPLY
  1. Copy every folder/file over your repository root (allow overwrites).
  2. Commit:  git add -A && git commit -m "Full-site performance and quality audit: reader fixes, sprite render path, mobile preview fix, CDN preconnect"
  3. Push:    git push origin main
  4. Watch the newest GitHub Actions run (must be green), then hard-refresh
     https://parsaetak.github.io/WEB/

VERIFY AFTER PUSH (60 seconds)
  - Home: RED MAGIC organism animates (red core, particles, pointer response)
  - Nav: all six scenes open; browser back/forward keeps scenes in sync
  - Library: filter counts correct; open RED MAGIC -> reader resumes at your
    last page; left strip shows pages NEAR the current page (sliding window
    "PAGES 198-207 / 270" style); jump to page 200 via the page box -> the
    strip slides with you; close and reopen -> still resumes
  - Phone width (~390px): library preview text and tag pills fit the screen
  - No console errors

NOTES
  - npm install is NOT needed (no dependency changes; pdfjs-dist stays banned).
  - Do NOT hand-edit data/library.json (synced at build time).
  - Remaining owner-side item: recompress page-2 artwork of RED MAGIC.pdf in
    the Contents repo (Books branch) — 10.24 MB single image = 85% of the
    file's cold-open cost. Code side is done; this is a content action.
